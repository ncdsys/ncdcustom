#execute this script only after cloning the remote repo first
#without clone the remote repo first, the origin and master will not be seet
# git startus  # shows all the tracked,untracked files
# git add <filename>  # adds the specified file to the local git
# git commit -m "comment"   # commits the added file to the local repo

git push origin master
